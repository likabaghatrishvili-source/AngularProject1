{
  "name": "amelie-inspired",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "ng serve -o",
    "server": "node server/server.js",
    "dev": "npx concurrently -n \"WEB,API\" -c \"auto,auto\" \"ng serve --port 4200\" \"node server/server.js\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
